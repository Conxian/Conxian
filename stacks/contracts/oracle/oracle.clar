;; oracle.clar;; Provides price feeds for various assets;; Traits;; Constants(use-trait oracle-trait .all-traits.oracle-trait)
(impl-trait oracle-trait)(define-constant ERR_UNAUTHORIZED (err u100))(define-constant ERR_INVALID_PRICE (err u101));; Data Maps(define-map asset-prices {  asset: principal} {  price: uint,  last-updated: uint});; Data Variables(define-data-var contract-owner principal tx-sender);; Public Functions(define-public (set-price (asset principal) (price uint))  (begin    (asserts! (is-eq tx-sender (var-get contract-owner)) ERR_UNAUTHORIZED)    (map-set asset-prices { asset: asset } { price: price, last-updated: block-height })    (ok true)  ));; Read-only Functions(define-read-only (get-price (asset principal))  (ok (map-get? asset-prices { asset: asset })))(define-read-only (get-contract-owner)  (ok (var-get contract-owner)))